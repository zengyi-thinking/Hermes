# ============================================
# Project Hermes - 环境变量配置模板
# ============================================

# ---- 应用配置 ----
HERMES_ENV=development
HERMES_LOG_LEVEL=INFO

# ---- 邮箱配置 (示例: Gmail) ----
EMAIL_IMAP_HOST=imap.gmail.com
EMAIL_IMAP_PORT=993
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_USE_SSL=true

# ---- LLM 配置 (第三方 API) ----
# Minimax
LLM_MINIMAX_API_KEY=your_minimax_api_key
LLM_MINIMAX_BASE_URL=https://api.minimax.chat/v1
LLM_MINIMAX_MODEL=abab6.5s-chat

# 智谱 GLM
LLM_GLM_API_KEY=your_glm_api_key
LLM_GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
LLM_GLM_MODEL=glm-4

# ---- LLM 选择 ----
LLM_PROVIDER=minimax  # minimax | glm | openai

# ---- Claude CLI 配置 ----
CLAUDE_CLI_PATH=claude
CLAUDE_WORK_DIR=D:\DevProject\Hermes
CLAUDE_TIMEOUT=600  # 秒

# ---- 任务队列配置 ----
TASK_POLL_INTERVAL=60  # 邮箱轮询间隔(秒)
TASK_MAX_RETRIES=3
TASK_TIMEOUT=300  # 单任务超时(秒)

# ---- 状态文件 ----
STATE_FILE=state/state.json

# ---- 报告生成配置 ----
# 报告模式: local | github-pages | server
REPORT_MODE=local
REPORT_OUTPUT_DIR=reports/
REPORT_TEMPLATE_DIR=templates/hermes/
REPORT_RETENTION_DAYS=7

# GitHub Pages 配置（可选）
GITHUB_REPO=owner/repo
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
GITHUB_BRANCH=main
GITHUB_REPORTS_PATH=reports/

# 报告服务器配置（可选）
REPORT_SERVER_HOST=0.0.0.0
REPORT_SERVER_PORT=8000
